<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Dynamic Category Strip Template - Fetches from Odoo Backend -->
    <template id="category_strip_complete" name="Category Strip Complete">
        <!-- Category Strip Container -->
        <div id="" class="category-strip-container">
            <div class="category-strip-inner">
                <div class="flex items-center justify-center relative">

                    <!-- Left Arrow Button -->
                    <button id="scroll-left" class="category-arrow category-arrow-left" type="button" style="display: none;">
                        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
                            <path d="m15 18-6-6 6-6"/>
                        </svg>
                    </button>

                    <!-- Categories Wrapper with Overflow Hidden -->
                    <div id="categories-wrapper" class="flex-1 overflow-hidden relative">
                        <!-- Categories Container - This will slide left/right -->
                        <div id="categories-container" class="flex gap-3 transition-transform duration-500 ease-in-out">

                            <!-- DYNAMIC CATEGORIES FROM ODOO BACKEND -->
                            <t t-set="categories" t-value="request.env['product.public.category'].sudo().search([('website_id', 'in', [False, request.website.id])], order='sequence, name')"/>

                            <t t-foreach="categories" t-as="category">
                                <t t-set="category_slug" t-value="category.name.lower().replace('&amp;', '-').replace(' ', '-').replace('--', '-').replace('--', '-').strip('-') + '-section'"/>

                                <a t-attf-href="#{category_slug}"
                                   class="category-item"
                                   t-attf-data-section="#{category_slug}">
                                    <t t-out="category.name"/>
                                </a>
                            </t>

                        </div>
                    </div>

                    <!-- Right Arrow Button -->
                    <button id="scroll-right" class="category-arrow category-arrow-right" type="button">
                        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <path d="m9 18 6-6-6-6"/>
                        </svg>
                    </button>

                </div>
            </div>
        </div>
    </template>
</odoo>