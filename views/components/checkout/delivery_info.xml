<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="delivery_info" name="Delivery Information Section">
        <section class="order-type-section bg-white rounded-xl shadow-sm border border-gray-200 p-6">
            <h2 class="section-title text-2xl font-bold text-gray-800 mb-6 flex items-center">
                <i class="fas fa-motorcycle text-primary mr-3"></i>
                Delivery Information
            </h2>

            <div class="delivery-info bg-primary/10 rounded-lg p-4 mb-6">
                <div class="flex items-start space-x-4">
                    <div class="bg-primary text-white p-3 rounded-lg">
                        <i class="fas fa-motorcycle text-xl"></i>
                    </div>
                    <div class="delivery-info-text">
                        <h4 class="font-semibold text-gray-800 mb-1">Delivery Available</h4>
                        <p class="text-gray-600 text-sm">We'll deliver your order to your doorstep. Delivery charges may apply based on your location.</p>
                    </div>
                </div>
            </div>

            <div class="form-group">
                <label for="address" class="block text-sm font-medium text-gray-700 mb-2">
                    Delivery Address <span class="text-red-500">*</span>
                </label>
                <textarea id="address" name="address" required="required" rows="4"
                          placeholder="Enter your complete address including area, street, house number"
                          class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-transparent transition-all resize-none"></textarea>
            </div>
        </section>
    </template>
</odoo>